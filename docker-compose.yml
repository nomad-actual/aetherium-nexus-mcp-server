services:
  mcp-server:
    image: ghcr.io/nomad-actual/aetherium-nexus-mcp-server:latest
    container_name: aetherium-nexus-mcp
    env_file:
      - .env
    environment:
      MCP_SERVER_HOST: 0.0.0.0
    ports:
      - "3030:3000"
    restart: unless-stopped
