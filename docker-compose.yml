version: '3.8'

services:
  mcp-server:
    build:
      context: ./
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "3000:3000"
    networks:
      - mcp-network

networks:
  mcp-network:
    driver: bridge
